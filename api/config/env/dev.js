(function() {
    "use strict";

    // Development environment configuration
    // This is just an object that contains environment-specific config options
    // like db connection string, session values, etc.
    module.exports = {
        'db': "mongodb://localhost/ecommerce"
    };
})();
